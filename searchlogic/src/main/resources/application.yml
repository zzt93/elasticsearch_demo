spring:
  data:
    elasticsearch:
      cluster-name: searcher
      cluster-nodes: "192.168.1.200:9300"
  sleuth:
    sampler:
      percentage: ${ZIPKIN_RATE:1}
  rabbitmq:
    host: ${RABBIT_ADDR:192.168.1.100}
    port: ${RABBIT_PORT:5672}
    username: ${ZIPKIN_USER:zipkin}
    password: ${ZIPKIN_PWD:zipkin}


